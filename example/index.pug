doctype html
html(lang="en")
  head
    meta(
      name="viewport"
      content="width=device-width, initial-scale=1.0"
    )
    title rocket example
  body
    ul
      each dir in dirs
        li
          a(href="/example/" + dir) #{dir}